@model IEnumerable<ASP.NET_MVC.Models.GENRE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { @class = "btn btn-primary" })
</p>
<table id="genreTable" class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.libelle)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.libelle)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.genre_id }, new { @class = "btn btn-primary" })
                @Html.ActionLink("Details", "Details", new { id = item.genre_id }, new { @class = "btn btn-default" })
                @Html.ActionLink("Delete", "Delete", new { id = item.genre_id }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>

<link href="//cdn.datatables.net/1.10.25/css/jquery.dataTables.min.css" rel="stylesheet" />
@section scripts{
    <script src="//cdn.datatables.net/1.10.25/js/jquery.dataTables.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#genreTable').DataTable();
        });
    </script>
}